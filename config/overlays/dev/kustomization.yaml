apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base

#namePrefix: dev-
commonLabels:
  variant: development


# replace the image tag of the container with latest
images:
- name: quay.io/wpernath/light-control:latest
  newName: quay.io/wpernath/light-control
  newTag: v1.0.0-beta3

# generate a configmap 
configMapGenerator:
- literals:
  - APP_GREETING=We are in DEVELOPMENT mode
  name: app-config

patches:
# This patch provides the name of the namespace into the env-value of the Job
# This is needed because we want to call the target rest service of person-service
# which is: light-control.$NAMESPACE.svc:8080
- patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/env/0/value
      value: light-dev
  target:
    kind: Job

# This patch is not necessary anymore, as we are not using nameprefix here.      
# this patch needs to be done, because kustomize does not change 
# the route target service name
#- patch: |-
#    - op: replace
#      path: /spec/to/name
#      value: dev-light-control
# target:
#   kind: Route
